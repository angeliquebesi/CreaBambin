import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  status: {
    danger: '#e53e3e',
  },
  palette: {
    primary: {
      main: '#14B8A6',
    },
    neutral: {
      main: '#64748B',
    },
  },
});

export default theme;